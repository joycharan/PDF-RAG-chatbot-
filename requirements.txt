PyMuPDF==1.23.17
gradio==4.11.0  # Using your currently installed version to avoid conflicts
langchain==0.0.325
langchain-community==0.0.16
Pillow==10.1.0
PyYAML==6.0.1
chromadb>=0.4.18
pypdf==4.0.0
Jinja2==3.1.3
sentence-transformers>=2.2.2
pydantic>=2.4.2
fsspec>=2023.10.0

# API Client packages - Google Gemini is the primary focus
google-generativeai>=0.3.0
langchain-google-genai>=0.0.5

# The following are no longer necessary for API-based approach
# Keep sentence-transformers as it's still needed for embeddings
torch>=2.0.1
transformers>=4.35.2
